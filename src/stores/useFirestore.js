import { defineStore } from 'pinia';

export const useFirestore = defineStore({
  id: 'firestoreStore',
  state: () => ({
    // values
  }),
  getters: {
    // get values from state
  },
  actions: {
    // modify values
  },
});
